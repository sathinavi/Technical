#include <stdio.h>

int main()
{
    int a, b, c, tri;
    printf("Enter the three numbers: ");
    scanf("%d %d %d", &a, &b, &c);

    tri = ((a==b && b==c) ? printf("triangle 1 is possible") :((a + b > c) ? printf("triangle 2 is possible") :printf("not possible")));

    return 0;
}
