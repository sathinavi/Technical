#include <stdio.h>

int main() {
    int number = 1000;
    int val = 1, sum = 0;

    while (number > 0) 
    {
        if (number % 10 == 1) 
        {
            sum += val;
        }
        val *= 2;
        number /= 10;
    }

    printf("Decimal value: %d", sum);
    return 0;
}

